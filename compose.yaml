services:
  devicepulse:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - devicepulse-data:/data
    restart: unless-stopped

volumes:
  devicepulse-data:
